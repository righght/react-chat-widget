const initialState = { unshiftIndex: 0, pushIndex: 0 };

function indexReducer(state = initialState, action) {
  switch (action.type) {
    case 'getUnshift':
      
      break;
    case 'getPush':

      break;
  }
}

export default indexReducer;
